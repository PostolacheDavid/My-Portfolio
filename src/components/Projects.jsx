import Slider from "./Slider";

const Projects = () => {
  return (
    <section id="projects" className="projects-section">
      <Slider />
    </section>
  );
};

export default Projects;
